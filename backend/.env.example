DATABASE_URL=sqlite:///local.db
JWT_SECRET_KEY=troque-por-uma-chave-forte
CORS_ORIGINS=http://127.0.0.1:3000,http://localhost:3000
HUGGINGFACE_API_KEY=hf_seu_token
HUGGINGFACE_MODEL=meta-llama/Meta-Llama-3-8B-Instruct
HUGGINGFACE_MAX_TOKENS=300
EXPOSE_RESET_TOKEN=1
SMTP_PROVIDER=gmail
SMTP_HOST=
SMTP_PORT=
SMTP_USERNAME=seu-email@provedor.com
SMTP_PASSWORD=sua-senha-ou-app-password
SMTP_FROM_EMAIL=seu-email@provedor.com
SMTP_FROM_NAME=ResolveAI
SMTP_USE_TLS=
SMTP_USE_SSL=

# Provedores suportados em SMTP_PROVIDER:
# gmail, outlook, hotmail, live, yahoo, zoho, protonmail, custom
# Para custom, preencha SMTP_HOST / SMTP_PORT / SMTP_USE_TLS / SMTP_USE_SSL manualmente.
